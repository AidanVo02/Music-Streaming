import React from 'react';
import { View, Text } from 'react-native';

const DetailScreen = () => (
  <View style={{ flex: 1, backgroundColor: '#111' }}>
    <Text style={{ color: '#fff' }}>Detail Screen</Text>
  </View>
);

export default DetailScreen;
